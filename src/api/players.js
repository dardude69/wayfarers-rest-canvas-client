import api from '.';
import url from 'url';

export default {

  createPlayer: async (username, password) => {
    
  },

  authenticatePlayer: async (username, password) => {
    // TODO:
    // encodeURIcomponent can be used on username
    // but really, I need a URL parser
  }

};
